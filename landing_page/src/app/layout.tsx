// Root layout: intentionally minimal.
// The [locale] layout provides the full <html> shell with lang + dir.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
